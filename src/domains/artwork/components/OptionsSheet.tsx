// This component is no longer used
// Options sheet is now directly managed in ArtworkDetailScreen

type Props = {
  visible: boolean;
  onClose: () => void;
  onReport: () => void;
};

export default function OptionsSheet(_props: Props) {
  // This component is deprecated and no longer used
  return null;
}
